<!-- FIREVIBE:BEGIN -->
> [!IMPORTANT]
> This project was generated by [FireVibe](https://firevibe.ai) and is
> actively edited by the FireVibe agent. If you are an agent or a human
> working on this code outside FireVibe, keep the project in a working
> state: `bun install`, `bunx tsc --noEmit`, and `bun run build` must
> pass. The dev server runs on port 8080 (`bun run dev`).

Conventions this project follows:

- `src/routeTree.gen.ts` is generated by TanStack Router — never edit it.
- `vite.config.ts` delegates to `@firevibe/vite-tanstack-config`. Do not
  re-add the plugins it already wires; duplicates break the build.
- Files that begin with `This file is automatically generated by
  FireVibe` are platform-managed — do not edit them directly.
- Routes are files under `src/routes/` (the conventions table lives in
  `src/routes/index.tsx`). The design system is CSS variables in
  `src/styles.css` using the shadcn token vocabulary — change tokens
  there and every component follows.
<!-- FIREVIBE:END -->
